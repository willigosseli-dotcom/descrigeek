[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/static/css/main.css"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
